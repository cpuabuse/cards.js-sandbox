# TODO: Populate this to the methods actually used
use:
  path: "use"
  methods:
    - "GET": 
    - "POST"
master:
  "/":
    allowed_methods:
      - "GET"
      - "POST"
  master_method1:
    "/":
      allowed_metods:
        - "GET"
  