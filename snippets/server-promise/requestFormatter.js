function requestFormatter(rawData){
	return new Promise(function(resolve, reject){
		
		resolve(javascriptObject)

		// if error BAD REQUEST
		reject(400);
	})
}

function requestFormatter_TSV(rawData){
	return new Promise(function(resolve, reject){
	
		resolve(javascriptObject)
	
		// if error BAD REQUEST
	reject(400);
    })
}
function requestFormatter_Int(rawData){
	return new Promise(function(resolve, reject){

		resolve(javascriptObject)

		// if error BAD REQUEST
		reject(400);		
	})
}
function requestFormatter_JSON(rawData){
	return new Promise(function(resolve, reject){
		
		resolve(javascriptObject)

		//if error BAD REQUEST
		reject(400);
	})
}
function requestFormatter_String(rawData){
	return new Promise(function(resolve){

		resolve(javascriptObject)

		//if error BAD REQUEST
		reject(400);
	})
}