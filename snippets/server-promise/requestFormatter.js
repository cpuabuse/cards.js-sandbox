function requestFormatter(rawData){
	return new Promise(function(resolve, reject){
		
		resolve(javascriptObject)

		// if error BAD REQUEST
		reject(400);
	})
}

requestFormatter_TSV();
requestFormatter_Int();
requestFormatter_JSON();
requestFormatter_String();