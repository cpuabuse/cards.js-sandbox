# Various settings to load
settings: # Defaults to "settings"
folders: # Defaults to "folders"
mysql: # Defaults to "mysql"
paths: # Defaults to "paths"
statusCodes: # Defaults to "statusCodes"
# errors: # Defaults to "systemErrorLevelTable"
templates: # Defaults to "templates"
behaviors: # Defaults to "behaviors"
events: # Defaults to "events"
resources: {} # Defaults to extension to resources/resources